[pytest]
addopts = -ra -v
python_files = test_*.py
markers =
    onboarding: onboarding and signup journey tests
    production: tests that run against production-like environments
    critical: critical path validations
